[package]
name = "alors"
version = "0.8.0"
edition = "2021"
description = "A CLI for your project: runs named tasks defined in a tasks.alors file."
license = "MIT"

[[bin]]
name = "alors"
path = "src/main.rs"
